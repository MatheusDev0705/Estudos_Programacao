let temp = 0;
    let resultado = Number(prompt("Digite um número:"));
    for(let i =1; i<=10; i++){
        let resultado = Number(prompt("Digite um número:"));
        if(resultado > temp) {
            temp = resultado
        }
        console.log(temp)
    }